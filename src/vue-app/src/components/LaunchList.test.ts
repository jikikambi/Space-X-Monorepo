// import { render, screen } from "@testing-library/vue";
// import LaunchList from "./LaunchList.vue";

// test("renders loading state", () => {
//   render(LaunchList, { props: { onSelect: () => {} } });
//   screen.getByText(/loading launches/i);
// });
